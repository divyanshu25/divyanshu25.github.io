# Resume PDF

The resume.pdf file should be generated using:
  make resume

And then committed to the repository so it can be served to users.

If resume.pdf is missing, run:
  make resume
  git add public/resume.pdf
  git commit -m "Add generated resume PDF"
